import React from 'react';
import useStyles from "./PostStyles"

const Post = () => {
    const classes = useStyles();

    return (
        <div>
            <h2>Each post</h2>
        </div>
    );
};

export default Post;